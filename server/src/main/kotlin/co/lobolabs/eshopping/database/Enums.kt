package co.lobolabs.eshopping.database

enum class ProductType { SIMPLE, CUSTOMIZABLE, COMPOSABLE }
enum class OrderItemType { SIMPLE, CUSTOM, COMPOSITE }
enum class PricingStrategy { MAX_PRICE, AVERAGE_PRICE, PROPORTIONAL }
